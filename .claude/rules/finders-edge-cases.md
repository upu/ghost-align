---
paths:
  - "src/finders.ts"
---

# src/finders.ts の検出ロジック変更はエッジケースを先に列挙する

`src/finders.ts` は正規表現・字句解析ベースでオペレーター/コロン/コメントなどを検出する。この手のヒューリスティックな判定は、実装後にレビューで一つずつ反例を指摘されて直す「もぐら叩き」になりやすい（ブロックコメントの挟み込み、行末コメント付きの区切り文字、隣接トークンの有無など）。

着手前に、対象トークンが取りうる周辺パターンを洗い出してからテストを書き、実装する:

- 行頭・行末のコメント（`//`, `/* */`, `#` など、対象言語で有効なもの）が対象トークンの前後に来る場合
- 対象トークンの直後に別の同種トークンが同じ行に続く場合（例: `case 1: obj = { a: 1 }`）
- 末尾のカンマ・セミコロンの有無によって構文上の意味が変わる場合
- 空白・タブの有無や複数個の並び

洗い出したケースをテストとして先に書き、実装で満たす。レビューで初めて反例が出るのではなく、実装前にテストで反例を潰しておく。
