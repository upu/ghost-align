{
  "name": "ghost-align",
  "displayName": "Ghost Align",
  "description": "Visually align operators like = without modifying your source code",
  "version": "1.3.0",
  "publisher": "upu",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/upu/ghost-align.git"
  },
  "homepage": "https://github.com/upu/ghost-align#readme",
  "bugs": {
    "url": "https://github.com/upu/ghost-align/issues"
  },
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#1e2330",
    "theme": "dark"
  },
  "keywords": [
    "align",
    "alignment",
    "formatter",
    "visual",
    "decoration",
    "operator",
    "ghost"
  ],
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ghostAlign.toggle",
        "title": "Ghost Align: Toggle"
      },
      {
        "command": "ghostAlign.copyAligned",
        "title": "Ghost Align: Copy with Alignment",
        "shortTitle": "Copy with Alignment"
      },
      {
        "command": "ghostAlign.toggleLanguage",
        "title": "Ghost Align: Disable/Enable for Current Language"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "ghostAlign.copyAligned",
          "group": "9_cutcopypaste",
          "when": "editorTextFocus"
        }
      ]
    },
    "configuration": {
      "title": "Ghost Align",
      "properties": {
        "ghostAlign.showStatusBar": {
          "type": "boolean",
          "default": false,
          "description": "Show a status bar item indicating Ghost Align ON/OFF (click to toggle)"
        },
        "ghostAlign.operators": {
          "type": "array",
          "default": [
            "="
          ],
          "description": "Operators to align (used when the current language is not listed in ghostAlign.operatorsByLanguage). Supported special tokens: \"=\" (assignment), \":\" (JSON/YAML key or CSS declaration), \"//\", \"#\", \"--\", and \";\" (trailing line comments — C-style, shell/Python/YAML, Lua/SQL/Haskell, and INI/asm respectively), \"=>\" (arrow function), \"\\\\\" (trailing line-continuation marker, e.g. shell/Makefile/C-preprocessor `#define` continuations — a `\\` only counts when it is the last non-whitespace character on the line, not one inside a string or escape sequence). For TypeScript/JavaScript, \":\" aligns type-annotation / object-literal colons (ternary and string/comment colons are excluded). The \"=\", \"=>\", and \"\\\\\" tokens exclude occurrences inside strings and comments. For \"//\", \"#\", \"--\", and \";\", only a *trailing* comment counts (code precedes it on the line): a whole-line comment is excluded, and for every marker other than \"//\" the marker must also be preceded by whitespace (so `x--y` or `a;b` are not mistaken for a comment). Any other string is matched literally, also excluding occurrences inside strings and comments. The list order defines both the priority and the left-to-right column order: each listed operator is aligned as its own column on the same line (e.g. [\"=\", \"#\"] aligns assignments and then trailing comments), and a later operator only matches occurrences after the previous column."
        },
        "ghostAlign.operatorsByLanguage": {
          "type": "object",
          "default": {
            "json": [
              ":"
            ],
            "jsonc": [
              ":"
            ],
            "yaml": [
              ":"
            ],
            "typescript": [
              ":",
              "="
            ],
            "typescriptreact": [
              ":",
              "="
            ],
            "javascript": [
              ":",
              "="
            ],
            "javascriptreact": [
              ":",
              "="
            ],
            "dotenv": [
              "="
            ],
            "properties": [
              "="
            ],
            "toml": [
              "="
            ],
            "ini": [
              "="
            ],
            "python": [
              "="
            ],
            "shellscript": [
              "="
            ],
            "ruby": [
              "=",
              "=>"
            ],
            "makefile": [
              "="
            ],
            "css": [
              ":"
            ],
            "scss": [
              ":"
            ],
            "less": [
              ":"
            ],
            "php": [
              "=",
              "=>"
            ],
            "rust": [
              "=",
              "=>"
            ],
            "go": [
              "="
            ],
            "lua": [
              "="
            ],
            "c": [
              "="
            ],
            "cpp": [
              "="
            ],
            "csharp": [
              "="
            ],
            "java": [
              "="
            ],
            "swift": [
              "="
            ],
            "kotlin": [
              "="
            ],
            "dart": [
              "="
            ],
            "zig": [
              "="
            ],
            "terraform": [
              "="
            ],
            "proto3": [
              "="
            ],
            "elixir": [
              "="
            ],
            "perl": [
              "="
            ],
            "sql": [
              "="
            ],
            "haskell": [
              "="
            ],
            "powershell": [
              "="
            ],
            "dockerfile": [
              "="
            ],
            "graphql": [
              ":"
            ],
            "scala": [
              "="
            ],
            "groovy": [
              "="
            ],
            "r": [
              "<-",
              "="
            ]
          },
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "Per-language operator overrides keyed by languageId (e.g. \"json\", \"jsonc\", \"typescript\"). If the current document's language is listed here, this list is used in place of ghostAlign.operators. Each value is a list of operators (see ghostAlign.operators for supported tokens such as \"=\", \":\", \"//\", \"#\", \"--\", \";\"); e.g. set \"typescript\": [\"//\"] to align trailing comments."
        },
        "ghostAlign.disabledLanguages": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Language IDs (e.g. \"markdown\", \"yaml\") for which Ghost Align decoration is fully disabled, including Markdown table / CSV / JSDoc alignment. Takes priority over ghostAlign.operatorsByLanguage: listing a language here disables it even if it also has an entry there. Equivalent to setting \"<languageId>\": [] in ghostAlign.operatorsByLanguage, but discoverable as its own setting."
        },
        "ghostAlign.alignUnknownLanguages": {
          "type": "boolean",
          "default": true,
          "description": "Align operators in languages that are not listed in ghostAlign.operatorsByLanguage, using the global ghostAlign.operators list (default [\"=\"]). Set to false to align only the languages explicitly listed in operatorsByLanguage — languages you add there yourself stay aligned. Unlike ghostAlign.disabledLanguages (an explicit blocklist), this switches off the fallback for every unlisted language at once. Markdown tables and CSV/TSV have their own alignment paths and are not affected."
        },
        "ghostAlign.jsdoc.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Align the parameter-name and description columns of consecutive JSDoc @param lines in JavaScript/TypeScript. Replaces the deprecated ghostAlign.alignJsdocParams, which is still honored while this key is left unset."
        },
        "ghostAlign.markdownTable.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Align Markdown table columns so the | separators line up. Set to false to turn off only the Markdown table alignment; to disable Ghost Align for Markdown entirely, use ghostAlign.disabledLanguages."
        },
        "ghostAlign.csv.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Align CSV/TSV columns so the delimiters line up. Set to false to turn off only the CSV/TSV alignment; to disable Ghost Align for those languages entirely, use ghostAlign.disabledLanguages."
        },
        "ghostAlign.csv.delimiters": {
          "type": "object",
          "default": {
            "csv": ",",
            "tsv": "\t"
          },
          "additionalProperties": {
            "type": "string"
          },
          "description": "Per-language CSV/TSV delimiter overrides keyed by languageId (e.g. \"csv\", \"tsv\"). A language listed here takes the CSV/TSV alignment path using its delimiter; add an entry for a language ID contributed by another extension (e.g. Rainbow CSV's \"csv (semicolon)\") to align it too. Each value must be exactly one character and not a double quote (the RFC 4180 quoting character); an invalid value falls back to the built-in default for csv/tsv, or is treated as not listed for any other language ID."
        },
        "ghostAlign.alignJsdocParams": {
          "type": "boolean",
          "default": true,
          "markdownDeprecationMessage": "**Deprecated**: use `#ghostAlign.jsdoc.enabled#` instead. This key is still honored while `ghostAlign.jsdoc.enabled` is left unset.",
          "description": "Align the parameter-name and description columns of consecutive JSDoc @param lines in JavaScript/TypeScript."
        },
        "ghostAlign.maxPadding": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Maximum number of ghost characters inserted per alignment column on a single line. For operator and JSDoc @param alignment, when aligning a group would require more, the extremely long outlier lines are excluded and the remaining lines are aligned without them. For Markdown table and CSV/TSV alignment, an outlier cell instead makes its whole column skip alignment (later columns keep aligning from each row's actual position), since excluding a row would leave the table shape broken. 0 means unlimited (default)."
        },
        "ghostAlign.ghostColor": {
          "type": "string",
          "default": "rgba(128, 128, 128, 0.25)",
          "description": "Tint color of the ghost padding (any CSS color string, e.g. \"rgba(128,128,128,0.25)\", \"#88888840\", \"transparent\"). Applied as both foreground and background so whitespace padding is distinguishable from real spaces. Leaving this empty in the settings UI falls back to the default; set \"transparent\" to disable tinting entirely."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node scripts/esbuild.js --production",
    "watch": "node scripts/esbuild.js --watch",
    "compile": "tsc -p ./",
    "check-types": "tsc --noEmit",
    "lint": "eslint src scripts eslint.config.mjs",
    "check:package": "npm run build && node scripts/check-package-contents.js",
    "test": "vscode-test",
    "test:scripts": "node --test \"scripts/*.test.js\"",
    "prepare-release": "node scripts/prepare-release.js",
    "package": "node -e \"require('fs').mkdirSync('dist',{recursive:true})\" && vsce package --out dist/ghost-align.vsix",
    "install:vsix": "code --install-extension dist/ghost-align.vsix --force",
    "package:install": "npm run package && npm run install:vsix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.1.1",
    "@types/vscode": "^1.125.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0"
  }
}
