{
  "name": "ghost-align",
  "displayName": "Ghost Align",
  "description": "Visually align operators like = without modifying your source code",
  "version": "0.2.0",
  "publisher": "upu",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/upu/ghost-align.git"
  },
  "homepage": "https://github.com/upu/ghost-align#readme",
  "bugs": {
    "url": "https://github.com/upu/ghost-align/issues"
  },
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#1e2330",
    "theme": "dark"
  },
  "keywords": [
    "align",
    "alignment",
    "formatter",
    "visual",
    "decoration",
    "operator",
    "ghost"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ghostAlign.toggle",
        "title": "Ghost Align: Toggle"
      }
    ],
    "configuration": {
      "title": "Ghost Align",
      "properties": {
        "ghostAlign.showStatusBar": {
          "type": "boolean",
          "default": false,
          "description": "Show a status bar item indicating Ghost Align ON/OFF (click to toggle)"
        },
        "ghostAlign.operators": {
          "type": "array",
          "default": [
            "="
          ],
          "description": "Operators to align (used when the current language is not listed in ghostAlign.operatorsByLanguage). Supported special tokens: \"=\" (assignment), \":\" (JSON/YAML key or CSS declaration), \"//\" and \"#\" (trailing line comments), \"=>\" (arrow function). For TypeScript/JavaScript, \":\" aligns type-annotation / object-literal colons (ternary and string/comment colons are excluded). The \"=\" and \"=>\" tokens exclude occurrences inside strings and comments. Any other string is matched literally. The list order defines both the priority and the left-to-right column order: each listed operator is aligned as its own column on the same line (e.g. [\"=\", \"#\"] aligns assignments and then trailing comments), and a later operator only matches occurrences after the previous column."
        },
        "ghostAlign.operatorsByLanguage": {
          "type": "object",
          "default": {
            "json": [
              ":"
            ],
            "jsonc": [
              ":"
            ],
            "yaml": [
              ":"
            ],
            "dotenv": [
              "="
            ],
            "properties": [
              "="
            ],
            "toml": [
              "="
            ],
            "ini": [
              "="
            ],
            "python": [
              "="
            ],
            "shellscript": [
              "="
            ],
            "ruby": [
              "=",
              "=>"
            ],
            "makefile": [
              "="
            ],
            "css": [
              ":"
            ],
            "scss": [
              ":"
            ],
            "less": [
              ":"
            ],
            "php": [
              "=",
              "=>"
            ],
            "rust": [
              "=",
              "=>"
            ],
            "go": [
              "="
            ],
            "lua": [
              "="
            ],
            "c": [
              "="
            ],
            "cpp": [
              "="
            ],
            "csharp": [
              "="
            ],
            "java": [
              "="
            ]
          },
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "Per-language operator overrides keyed by languageId (e.g. \"json\", \"jsonc\", \"typescript\"). If the current document's language is listed here, this list is used in place of ghostAlign.operators. Each value is a list of operators (see ghostAlign.operators for supported tokens such as \"=\", \":\", \"//\", \"#\"); e.g. set \"typescript\": [\"//\"] to align trailing comments."
        },
        "ghostAlign.alignJsdocParams": {
          "type": "boolean",
          "default": true,
          "description": "Align the parameter-name and description columns of consecutive JSDoc @param lines in JavaScript/TypeScript."
        },
        "ghostAlign.maxPadding": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Maximum number of ghost characters inserted per alignment column on a single line. When aligning a group would require more, the extremely long outlier lines are excluded and the remaining lines are aligned without them. 0 means unlimited (default). Does not apply to Markdown table alignment."
        },
        "ghostAlign.ghostCharacter": {
          "type": "string",
          "default": " ",
          "description": "Character used for ghost padding. Defaults to a non-breaking space (U+00A0), which renders like a half-width space but is not collapsed by the editor's rendering. Plain ASCII spaces will be collapsed to one cell and break alignment — use NBSP or en-space (U+2002) if you customize this. Leaving this empty in the settings UI falls back to the default."
        },
        "ghostAlign.ghostColor": {
          "type": "string",
          "default": "rgba(128, 128, 128, 0.25)",
          "description": "Tint color of the ghost padding (any CSS color string, e.g. \"rgba(128,128,128,0.25)\", \"#88888840\", \"transparent\"). Applied as both foreground and background so whitespace padding is distinguishable from real spaces. Leaving this empty in the settings UI falls back to the default; set \"transparent\" to disable tinting entirely."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node scripts/esbuild.js --production",
    "watch": "node scripts/esbuild.js --watch",
    "compile": "tsc -p ./",
    "check-types": "tsc --noEmit",
    "check:package": "npm run build && node scripts/check-package-contents.js",
    "test": "vscode-test",
    "package": "node -e \"require('fs').mkdirSync('dist',{recursive:true})\" && vsce package --out dist/ghost-align.vsix",
    "install:vsix": "code --install-extension dist/ghost-align.vsix --force",
    "package:install": "npm run package && npm run install:vsix"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.3.0"
  }
}
