{
  "name": "ghost-align",
  "displayName": "Ghost Align",
  "description": "Visually align operators like = without modifying your source code",
  "version": "0.0.1",
  "publisher": "upu",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/upu/ghost-align.git"
  },
  "homepage": "https://github.com/upu/ghost-align#readme",
  "bugs": {
    "url": "https://github.com/upu/ghost-align/issues"
  },
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#1e2330",
    "theme": "dark"
  },
  "keywords": [
    "align",
    "alignment",
    "formatter",
    "visual",
    "decoration",
    "operator",
    "ghost"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Formatters",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "ghostAlign.toggle",
        "title": "Ghost Align: Toggle"
      }
    ],
    "configuration": {
      "title": "Ghost Align",
      "properties": {
        "ghostAlign.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable visual alignment"
        },
        "ghostAlign.operators": {
          "type": "array",
          "default": [
            "="
          ],
          "description": "Operators to align (used when the current language is not listed in ghostAlign.operatorsByLanguage)"
        },
        "ghostAlign.operatorsByLanguage": {
          "type": "object",
          "default": {
            "json": [
              ":"
            ],
            "jsonc": [
              ":"
            ]
          },
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "Per-language operator overrides keyed by languageId (e.g. \"json\", \"jsonc\", \"typescript\"). If the current document's language is listed here, this list is used in place of ghostAlign.operators."
        },
        "ghostAlign.ghostCharacter": {
          "type": "string",
          "default": " ",
          "description": "Character used for ghost padding. Defaults to a non-breaking space (U+00A0), which renders like a half-width space but is not collapsed by the editor's rendering. Plain ASCII spaces will be collapsed to one cell and break alignment — use NBSP or en-space (U+2002) if you customize this. Leaving this empty in the settings UI falls back to the default."
        },
        "ghostAlign.ghostColor": {
          "type": "string",
          "default": "rgba(128, 128, 128, 0.25)",
          "description": "Tint color of the ghost padding (any CSS color string, e.g. \"rgba(128,128,128,0.25)\", \"#88888840\", \"transparent\"). Applied as both foreground and background so whitespace padding is distinguishable from real spaces. Leaving this empty in the settings UI falls back to the default; set \"transparent\" to disable tinting entirely."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "vscode-test",
    "package": "vsce package --out ghost-align.vsix",
    "install:vsix": "code --install-extension ghost-align.vsix --force",
    "package:install": "npm run package && npm run install:vsix"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.9.1",
    "typescript": "^5.3.0"
  }
}
